## Packages
recharts | For financial charts and data visualization
date-fns | For date formatting and manipulation in Polish locale
framer-motion | For smooth page transitions and UI animations
clsx | For conditional class names
tailwind-merge | For merging tailwind classes

## Notes
Integration with Replit Auth is active.
Dashboard requires data from /api/stats/dashboard.
Excel upload uses /api/import endpoint.
Currency display must be formatted as PLN.
All UI text must be in Polish.
